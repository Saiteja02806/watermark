{
  "name": "local-video-cleaner",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @local-video-cleaner/web",
    "build": "npm run build --workspace @local-video-cleaner/web",
    "test:web": "npm run test --workspace @local-video-cleaner/web"
  },
  "dependencies": {
    "ffmpeg-static": "^5.2.0",
    "ffprobe-static": "^3.1.0"
  }
}

